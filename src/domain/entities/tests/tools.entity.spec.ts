/* import { Tool, ToolProps } from "../tools.entity";

describe('Tool Tests', () => {

    test('constructor', () => {
        let toolProps: ToolProps = {
            id: 1,
            title: 'tool exemple',
            link: 'toolexempe.com',
            description: 'Tool exemple for a simple test constructor',
        }

        let tool = new Tool(toolProps);
        expect(tool.props).toStrictEqual({
            ...toolProps, tags: []
        })

        toolProps = {
            id: 1,
            title: 'tool exemple',
            link: 'toolexempe.com',
            description: 'Tool exemple for a simple test constructor',
            tags: ['exempe tag', 'other exemple tag'],
        }

        tool = new Tool(toolProps);
        expect(tool.props).toStrictEqual({
            ...toolProps, tags: ['exempe tag', 'other exemple tag']
        })
    })
}) */
